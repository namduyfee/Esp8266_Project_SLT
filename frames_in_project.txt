
TCP:

delete, open, close:
- 'TCP' + CMD (D O C)
-> trả về 'TCP' + CMD (RETURN D O C) + state

read
- 'TCP' + CMD (READ) + offset + size to read
-> trả về 'TCP' + CMD (RETURN READ) + state (1 | 0) +  if state = 1 (offset + size + data) + checksum (only data)

wirte
- 'TCP' + CMD (WRITE) + offset + size + data + checksum (only data)
- data 
- data 
...
-> trả về 'TCP' + CMD (RETURN WRITE) + state (1 | 0)

ESPNOW:
form:
NOW + CMD + payload + checksum


* if CMD = open close delete
sender retry + timeout: NOW + CMD + checksum

receiver retry + timeout -> NOW + CMD + state (1 | 0) + checksum

* if CMD = read
sender retry + timeout: NOW + CMD (READ) + offset + size + checksum

receiver retry + timeout -> NOW + CMD (RETURN READ) + tổng số gói sẽ gửi + state (1 | 0) + offset + size + checksum
sender 				  : NOW + CMD (ACK) + checksum
receiver: 
- gửi liên tục segment:
stt + data + checksum
...
- retry + timeout : NOW + CMD (end frame) + checksum
Sender:
- NOW + ACK | NACK + if NACK [array segment resend] + checksum

+ if ACK ; retry + timeout 
+ if NACK; retry + timerout | recv new segment

* if WRITE

sender retry + timeout : NOW + ST_WRITE + checksum 
-> NOW + ACK + WRITE + checksum 

NOW + CMD (WRITE) + offset + data + checksum
recev 				  : NOW + ACK + checksum

sender: 
- gửi liên tục segment:
stt + offset + size + data + checksum
...
- retry + timeout : NOW + CMD (end frame) + checksum
Recv:
- NOW + ACK | NACK + if NACK [array segment resend] + checksum

+ if ACK ; retry + timeout
+ if NACK; retry + timerout | recv new segment


- cần 1 biến để biết busy; mối peer sẽ được cấp 1 biến này tương ứng 
- một biến lưu mode gửi; mỗi peer cx có biến này. 

- quét qua tất cả các add và đọc busy của mối peer nếu mode_send khác none thì 

câu hỏi là nên gửi 1 hay nhiều peer tại một chu kì truyền. 

- cách thức 2 gửi queue : 

if( read_queue == pass) 
{

}








- truyền các yêu cầu cơ bản.



























